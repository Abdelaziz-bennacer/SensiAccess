import 'package:flutter/material.dart';

const Color kLightTitle = Color(0xFFFFFFFF);
const Color kDarkTitle = Color(0xFF000000);

const Text kHomePageTitle = Text(
  "Sensi'access",
  semanticsLabel:
      "Sensi'access, l'application de sensibilisation à l'accessibilité numérique",
  style: TextStyle(fontSize: 20),
);
const Text kNavAuditiveTitle = Text(
  'La Navigation Auditive',
  style: TextStyle(fontSize: 20),
);
const Text kPageCouleurTitle = Text(
  'Les Couleurs/Le Daltonisme',
  style: TextStyle(fontSize: 20),
);
const Text kGoodFormTitle = Text(
  'Formulaire accessible',
  style: TextStyle(fontSize: 20),
);
